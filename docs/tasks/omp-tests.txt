你是 Phase 2 的测试工程师。请先读 docs/contract.md 第8节测试场景清单，以及 docs/opencode-data-format.md。你只写测试，不写实现。

为 src-tauri/src/collector/ 编写 Rust 测试，覆盖契约第8节的全部场景：

本地 SQLite 采集测试，用真实的临时 db 文件（用 tempfile crate 建临时目录，在里面建 session 表并插入样例行），测试 resolve_opencode_db、read_all_sessions、aggregate_local 三个函数。覆盖场景：无 OpenCode 环境时 resolve 返回 NotFound；数据为空时 aggregate 返回全零结构不 panic；数据损坏（非 SQLite 文件或缺列）时返回 OpenFailed 或 QueryFailed；多 session 时 aggregate 正确累加且按 model 正确分组；时间窗口分桶，给定固定 now_ms 和若干 time_created，验证 7 天和 30 天桶归属正确；目录不可读时返回 OpenFailed 不崩溃；model JSON 解析，合法 JSON 和缺 variant 的 JSON 都能容错。

opencode.ai API 测试，用 mock HTTP server（用 httpmock crate），端点先占位为 /quota 和 /account，测试：有 cookie 且 API 返回配额时正确解析成 ApiQuota；无 cookie 时返回 NoCookie；cookie 失效返回 401 时得到 Unauthorized；API 超时或 5xx 时得到 ApiError；配额缓存生效，重复调用在刷新间隔内不重复打 API。

测试放在 src-tauri/tests/ 目录下，按场景分文件。需要加的测试依赖 tempfile 和 httpmock，写进 Cargo.toml 的 dev-dependencies 段。

注意：你写的测试现在编译会失败，因为实现还没好，这是 TDD 的正常状态。你按契约里定义的函数签名写，实现完成后测试即通过。不要为了让测试编译通过去改契约或写桩实现。

完成后在终端输出测试文件清单和覆盖的场景列表。遇到契约不清楚的地方，在输出里标注疑问，不要自行猜测。
