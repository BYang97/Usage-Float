# Usage-Float 桌面应用测试方案

> 版本：0.1.0  
> 基于 Tauri 2 双窗口架构  
> 不修改代码，仅设计测试方案

---

## 目录

- [一、窗口测试](#一窗口测试)
- [二、数据流测试](#二数据流测试)
- [三、异常测试](#三异常测试)
- [四、性能测试](#四性能测试)
- [五、测试记录模板](#五测试记录模板)

---

## 一、窗口测试

### 1.1 架构背景

当前窗口定义（`tauri.conf.json`）：

| 窗口 | label | 尺寸 | 装饰 | 顶层 | 透明 | URL |
|------|-------|------|------|------|------|-----|
| 主窗口 | `main` | 1200×800 | 是 | 否 | 否 | `/` |
| 悬浮球 | `float` | 340×200 | 否 | 是 | 是 | `/?window=float` |

窗口路由逻辑（`main.tsx`）：根据 `?window=float` 参数渲染 FloatWidget 独立实例或完整 App。

---

### 1.2 测试用例

#### TC-W01：主窗口启动

| 项目 | 内容 |
|------|------|
| **前置条件** | Tauri 应用已构建，OpenCode 无需运行 |
| **测试步骤** | 1. 执行 `cargo run` 或运行构建产物<br>2. 观察主窗口出现 |
| **预期结果** | - 主窗口 1200×800 正常显示<br>- 标题栏显示「OpenCode Usage Float」<br>- 仪表盘完整渲染：侧栏、Header、PlanCard、3 个 QuotaCard、TokenUsage、ModelUsage<br>- 无 JS 控制台错误 |
| **问题记录** | 窗口尺寸是否准确？仪表盘是否完整？Header 按钮（设置/最小化/关闭）是否可点击？ |

#### TC-W02：悬浮球窗口启动

| 项目 | 内容 |
|------|------|
| **前置条件** | Tauri 应用已运行 |
| **测试步骤** | 1. 应用启动后，观察悬浮球窗口是否同时出现 |
| **预期结果** | - 340×200 无边框窗口出现在屏幕右上角附近<br>- 窗口始终置顶（覆盖在其他应用之上）<br>- 背景透明，仅 FloatWidget 内容可见<br>- 显示 ProviderBadge、QuotaRing、额度百分比、重置时间、「打开仪表盘 >」链接 |
| **问题记录** | 无边框窗口能否正常显示？透明背景是否生效（不应有深色方块）？置顶效果是否始终有效？ |

#### TC-W03：主窗口关闭

| 项目 | 内容 |
|------|------|
| **前置条件** | 主窗口已打开，悬浮球窗口已打开 |
| **测试步骤** | 1. 点击 Header 右上角 ✕ 按钮<br>2. 或按 Alt+F4 |
| **预期结果** | - 两个窗口同时关闭，进程退出<br>- 无崩溃弹窗<br>- 操作系统任务栏中应用消失 |
| **问题记录** | 关闭是否影响悬浮球？进程是否完全退出（检查任务管理器）？ |

#### TC-W04：悬浮球关闭

| 项目 | 内容 |
|------|------|
| **前置条件** | 两个窗口均已打开 |
| **测试步骤** | 1. 点击悬浮球右上角 ✕ 按钮 |
| **预期结果** | - 仅悬浮球窗口关闭<br>- 主窗口保持打开<br>- 应用中 FloatWidget 组件隐藏（App.tsx 中 `floatVisible` 变为 false） |
| **问题记录** | 主窗口中的 FloatWidget 叠加层是否也同步消失？（当前逻辑：两个窗口独立，不会同步） |

#### TC-W05：窗口重新打开（悬浮球）

| 项目 | 内容 |
|------|------|
| **前置条件** | 悬浮球已关闭，主窗口仍在运行 |
| **测试步骤** | 1. 在主窗口界面寻找「重新打开悬浮球」的入口<br>2. 或检查任务栏托盘 |
| **预期结果** | - **当前缺陷**：应用中无重新打开悬浮球的入口<br>- 预期设计：设置页应有开关，或系统托盘图标可重新打开<br>- 验证：至少主窗口不崩溃 |
| **问题记录** | 当前架构是否有悬浮球恢复机制？（无）此场景需后续功能补全。 |

#### TC-W06：窗口拖动

| 项目 | 内容 |
|------|------|
| **前置条件** | 悬浮球窗口已打开，主窗口已打开 |
| **测试步骤** | 1. 悬浮球：拖拽窗口任意位置<br>2. 主窗口：拖拽标题栏 |
| **预期结果** | - 悬浮球：跟随鼠标平滑移动，松开后停在目标位置<br>- 主窗口：正常 OS 拖拽行为<br>- 悬浮球拖拽到屏幕边缘时不应超出可见区域 |
| **问题记录** | 悬浮球拖拽边界是否限制在屏幕内？拖动后位移是否异常？多显示器场景下拖拽是否正常？ |

#### TC-W07：窗口状态同步

| 项目 | 内容 |
|------|------|
| **前置条件** | 主窗口和悬浮球窗口均已打开 |
| **测试步骤** | 1. 在主窗口仪表盘查看额度数据<br>2. 观察悬浮球窗口的额度数据 |
| **预期结果** | - **当前行为**：两个窗口数据不同步<br>  - 主窗口 FloatWidget：从 `App.tsx` 的 `quota` state 获取（通过 `getQuota()` → service → provider）<br>  - 独立悬浮球窗口：硬编码 82%（`main.tsx` 第 17 行）<br>- 确认这是已知问题：独立浮窗尚未接入数据流 |
| **问题记录** | 两个浮窗实例的数据源不同，属于架构待办项。跨窗口状态同步需后续通过 Tauri event 或 shared state 实现。 |

**窗口状态同步矩阵：**

| 场景 | 主窗口 FloatWidget | 独立浮窗窗口 |
|------|-------------------|-------------|
| 数据来源 | `usage-service.getQuota()` | 硬编码常量 |
| 关闭操作 | `setFloatVisible(false)`（隐藏） | `window.close()`（销毁窗口） |
| 打开仪表盘 | 跳转仪表盘 + 隐藏浮窗 | 空实现 |
| 数据刷新 | 跟随 App 刷新 | 永不刷新 |

---

## 二、数据流测试

### 2.1 架构背景

```
Rust command (mock data)
    ↓ Tauri IPC Bridge (JSON serialization/deserialization)
TauriProvider.invoke('get_account_info')
    ↓
usage-service.ts (cache layer)
    ↓
React components (Dashboard / FloatWidget)
```

关键路径：

```
getUsageData()
  → cached 检查（有缓存直接返回）
  → provider.getUsageData()
    → TauriProvider:
      → Promise.all([
          invoke('get_account_info'),
          invoke('get_quota_info'),
          invoke('get_token_records')
        ])
    → MockProvider:
      → 直接返回 mock.ts 数据
```

### 2.2 测试用例

#### TC-D01：正常数据流

| 项目 | 内容 |
|------|------|
| **前置条件** | Tauri 应用运行，Rust command 正常 |
| **测试步骤** | 1. 主窗口启动完成<br>2. 观察仪表盘所有数据区域 |
| **预期结果** | - PlanCard 显示「Go 月度版 / active / 2026-08-20」<br>- QuotaCard 显示 5 小时 82%/本周 63%/本月 45%<br>- TokenUsage 显示 8.5M/42M/180M 及 7 天趋势图<br>- ModelUsage 显示 GPT 系列 60% / Claude 系列 40%<br>- 悬浮球显示 82%（硬编码） |
| **问题记录** | 每个数据块的取值是否与 `mock.ts` / Rust command 一致？ |

#### TC-D02：数据刷新

| 项目 | 内容 |
|------|------|
| **前置条件** | 仪表盘已加载完整数据 |
| **测试步骤** | 1. 修改 Rust command 返回新数据（如将 plan 改为「Pro 年度版」）<br>2. 重启应用<br>3. 或调用 `refreshCache()` 后重新加载 |
| **预期结果** | - 新数据正确显示<br>- 仪表盘无残留旧数据 |
| **问题记录** | `refreshCache()` 当前未被任何 UI 操作触发。应用仅启动时加载一次数据。需确认：启动时是否总是获取新数据？ |

**当前缓存行为分析：**

```typescript
// usage-service.ts
let cached: UsageData | null = null;

export async function getUsageData(): Promise<UsageData> {
  if (!cached) {
    cached = await provider.getUsageData();  // 仅首次调用
  }
  return cached;
}

export function refreshCache() {
  cached = null;  // 清空缓存，下次调用重新获取
}
```

- **问题**：`refreshCache()` 在项目中没有任何调用方
- **影响**：仪表盘数据仅在**首次渲染时加载一次**，之后永久使用缓存
- **建议**：添加定时刷新（设置页的刷新频率选项）或 IPC event 驱动刷新

#### TC-D03：IPC 通信验证

| 项目 | 内容 |
|------|------|
| **前置条件** | Tauri 应用运行中 |
| **测试步骤** | 1. 打开 Tauri DevTools（右键 → Inspect）<br>2. 在 Console 中执行：<br>```js
const { invoke } = await import('@tauri-apps/api/core');
const account = await invoke('get_account_info');
console.log(account);
const quota = await invoke('get_quota_info');
console.log(quota);
const tokens = await invoke('get_token_records');
console.log(tokens);
``` |
| **预期结果** | - 三个 invoke 均成功返回 JSON<br>- 返回字段名使用 camelCase（`plan`, `status`, `fiveHourPercent`...）<br>- 类型与 TypeScript 接口一致 |
| **问题记录** | 字段名大小写是否与 TypeScript `types/index.ts` 对齐？返回类型是否精准匹配？ |

#### TC-D04：IPC 失败 - 单个 command 超时/崩溃

| 项目 | 内容 |
|------|------|
| **前置条件** | 应用运行，模拟 Rust command 抛出异常 |
| **测试步骤** | 1. 修改 `lib.rs` 中 `get_account_info` 添加 `panic!()`<br>2. 重启应用<br>3. 观察仪表盘 |
| **预期结果** | - **当前行为**：`Promise.all()` 中任何一个 Promise reject 将导致整体失败<br>- `getUsageData()` 抛出未捕获异常<br>- 仪表盘 `useEffect` 中 `.then(setAccount)` 不会执行<br>- 页面显示空白（Dashboard 返回 null） |
| **问题记录** | 当前无错误边界。建议：<br>- 每个 invoke 独立 try/catch<br>- 失败时返回部分数据 + fallback<br>- Dashboard 添加 ErrorBoundary |

#### TC-D05：IPC 失败 - command 不存在

| 项目 | 内容 |
|------|------|
| **前置条件** | 应用运行 |
| **测试步骤** | 1. 执行 `invoke('nonexistent_command')` |
| **预期结果** | - Tauri 抛出 `-32601 Method not found` 错误<br>- 前端未捕获此错误，console 显示未处理 rejection |
| **问题记录** | 所有 invoke 调用均缺少 `.catch()` 处理。需要全局异常兜底。 |

#### TC-D06：IPC 失败 - 权限不足

| 项目 | 内容 |
|------|------|
| **前置条件** | 应用运行 |
| **测试步骤** | 1. 从 `capabilities/default.json` 中移除 `core:default`<br>2. 重启应用 |
| **预期结果** | - Tauri 拒绝 IPC 调用<br>- invoke 返回权限错误<br>- 仪表盘空白 |
| **问题记录** | 权限配置变更测试需在开发环境验证。生产环境不会出现此情况。 |

---

## 三、异常测试

### 3.1 异常场景矩阵

以下场景针对**后续真实数据采集阶段**（当前为 mock 数据，不会触发）：

| ID | 场景 | 触发条件 | 预期行为 | 当前状态 |
|----|------|---------|---------|---------|
| TC-E01 | 无 OpenCode 数据文件 | `opencode.db` 不存在 | 提供降级数据，不崩溃 | 当前 mock 数据不受影响 |
| TC-E02 | SQLite 被占用 | OpenCode 正在运行，持有写锁 | 设置 `busy_timeout` 优雅等待或提示 | 尚未实现 SQLite 读取 |
| TC-E03 | 数据库损坏 | `opencode.db` 文件损坏 | 捕获 `rusqlite` 错误，返回降级数据 | 尚未实现 SQLite 读取 |
| TC-E04 | 网络不存在 | 离线环境（当前不依赖网络） | 不受影响 | 应用纯本地，无网络依赖 |
| TC-E05 | 用户权限不足 | 以受限用户运行，无法读取 `~/.local/share/opencode/` | 捕获文件权限错误，显示提示 | 尚未实现文件读取 |

### 3.2 详细用例

#### TC-E01：数据库不存在

| 项目 | 内容 |
|------|------|
| **前置条件** | `~/.local/share/opencode/opencode.db` 不存在或已被删除 |
| **测试步骤** | 1. 在无 OpenCode 的环境中启动应用 |
| **预期结果** | - 应用不应崩溃<br>- 应显示「未检测到 OpenCode 数据」提示或降级 UI<br>- 不应出现空白窗口 |
| **当前实现** | TauriProvider 无错误处理，`invoke` 失败将导致未捕获异常 |
| **Bug 等级** | P0 - 阻断 |

#### TC-E02：数据库被占用

| 项目 | 内容 |
|------|------|
| **前置条件** | OpenCode 正在运行并持有 SQLite 连接 |
| **测试步骤** | 1. 启动 OpenCode<br>2. 启动 Usage-Float<br>3. 观察应用行为 |
| **预期结果** | - 应用不应崩溃<br>- 应提示「OpenCode 正在运行，无法读取数据」<br>- 或设置短超时后返回部分数据 |
| **当前实现** | 尚无 SQLite 读取逻辑 |
| **Bug 等级** | P1 - 严重 |

#### TC-E03：数据库损坏

| 项目 | 内容 |
|------|------|
| **前置条件** | `opencode.db` 文件头损坏 |
| **测试步骤** | 1. 手动破坏文件前几个字节<br>2. 启动应用 |
| **预期结果** | - `rusqlite::Connection::open()` 返回 `Error::InvalidPath` 或 `DatabaseCorrupt`<br>- 应用应捕获此错误并显示友好提示 |
| **当前实现** | 尚无 SQLite 读取逻辑 |
| **Bug 等级** | P1 - 严重 |

#### TC-E04：离线环境

| 项目 | 内容 |
|------|------|
| **前置条件** | 无网络连接 |
| **测试步骤** | 1. 断开网络<br>2. 启动应用 |
| **预期结果** | - 应用完全正常运行<br>- 所有功能不受影响（当前不依赖网络） |
| **当前实现** | 所有数据来自本地 mock / 本地 SQLite，无需网络 |
| **Bug 等级** | P3 - 建议 |

#### TC-E05：权限不足

| 项目 | 内容 |
|------|------|
| **前置条件** | 以低权限用户运行 |
| **测试步骤** | 1. 创建一个无 `~/.local/share/opencode/` 读取权限的用户<br>2. 启动应用 |
| **预期结果** | - 文件读取返回 `PermissionDenied`<br>- 应用显示「无法读取 OpenCode 数据目录，请检查权限」 |
| **当前实现** | 尚无文件读取逻辑 |
| **Bug 等级** | P2 - 中等 |

### 3.3 通用异常要求

所有异常场景必须满足：

```
✅ 应用不崩溃（no panic / no uncaught exception）
✅ 窗口不白屏（不返回 null）
✅ 用户可见提示（toast / 状态栏 / 错误页）
✅ 可恢复（错误消除后能正常使用）
❌ 不泄露敏感信息（不显示文件路径、SQL 语句）
```

---

## 四、性能测试

### 4.1 测试环境

| 项目 | 标准 |
|------|------|
| **操作系统** | Windows 10/11 64-bit |
| **CPU** | Intel i5-12400 或同等（6C12T） |
| **内存** | 16 GB DDR4 |
| **磁盘** | NVMe SSD |
| **屏幕** | 1920×1080, 100% DPI |
| **OpenCode** | 已安装且有 200+ 条会话记录 |

### 4.2 性能指标

#### TC-P01：启动时间

| 指标 | 目标 | 警告线 | 失败线 |
|------|------|--------|--------|
| 冷启动（首次运行） | ≤ 3s | 3-5s | > 5s |
| 热启动（已缓存） | ≤ 1.5s | 1.5-3s | > 3s |
| Dashboard 首帧渲染 | ≤ 1s（数据加载后） | 1-2s | > 2s |
| 悬浮球首帧渲染 | ≤ 0.5s | 0.5-1s | > 1s |

**测量方式：**
- 冷启动：从双击 exe 到 Dashboard 完整渲染
- 热启动：关闭后立即重新打开
- 使用 Tauri `devtools` performance tab 或 `performance.now()` 打点

#### TC-P02：CPU 占用

| 场景 | 目标 | 警告线 | 失败线 |
|------|------|--------|--------|
| 空闲（无操作） | ≤ 0.5% | 0.5-2% | > 2% |
| 仪表盘展示 | ≤ 1% | 1-3% | > 3% |
| 悬浮球拖动 | ≤ 5% | 5-10% | > 10% |
| 数据刷新（后台） | ≤ 3% | 3-8% | > 8% |

**测量方式：** 任务管理器 / `Get-Process` PowerShell

#### TC-P03：内存占用

| 场景 | 目标 | 警告线 | 失败线 |
|------|------|--------|--------|
| 空闲常驻 | ≤ 80 MB | 80-120 MB | > 120 MB |
| 仪表盘全量显示 | ≤ 100 MB | 100-150 MB | > 150 MB |
| 后台运行 24h | ≤ 120 MB（无泄漏） | > 120 MB | 持续增长 > 200 MB |

**测量方式：** 任务管理器「内存(专用工作集)」列 / Windows Performance Monitor

#### TC-P04：后台运行稳定性

| 指标 | 目标 | 警告线 | 失败线 |
|------|------|--------|--------|
| 最小化 1 小时后 CPU | ≤ 0.5% | 0.5-2% | > 2% |
| 最小化 1 小时后内存 | 无增长 | 增长 < 10 MB | 增长 > 20 MB |
| 24 小时无崩溃率 | 100% | - | 任意崩溃 |
| 悬浮球 24 小时置顶稳定 | 始终可见 | - | 消失 / 偏移 |

**测试方式：**
1. 启动应用，确认主窗口 + 悬浮球正常
2. 最小化主窗口（保留悬浮球置顶）
3. 正常使用电脑 24 小时
4. 每 1 小时检查：
   - 悬浮球是否仍在正确位置
   - CPU/内存是否异常增长
   - 点击悬浮球按钮是否响应

### 4.3 性能回归触发条件

以下情况必须重新执行完整性能测试：

- Rust command 逻辑变更（从 mock 切换到真实数据读取）
- 新增数据聚合查询
- React 组件重构或新增
- Tauri 或 WebView2 运行时版本升级

---

## 五、测试记录模板

### 5.1 单次测试记录格式

```
┌─────────────────────────────────────────────────────┐
│              Usage-Float 测试报告                     │
│              版本：0.1.0    日期：____-__-__          │
│              测试人：________                        │
├─────────────────────────────────────────────────────┤
│                                                     │
│  测试环境                                           │
│  ─────────────────────────────────────────────────  │
│  OS:                                                │
│  Rust:                                              │
│  WebView2:                                          │
│  OpenCode 版本:                                     │
│  OpenCode 会话数:                                   │
│                                                     │
│  ┌───────┬──────────┬──────────┬──────────┬──────┐  │
│  │ ID    │ 测试项目  │ 步骤     │ 预期结果  │ 状态 │  │
│  ├───────┼──────────┼──────────┼──────────┼──────┤  │
│  │ TC-W01│          │          │          │      │  │
│  │ TC-W02│          │          │          │      │  │
│  │ ...   │          │          │          │      │  │
│  └───────┴──────────┴──────────┴──────────┴──────┘  │
│                                                     │
│  Bug 清单                                           │
│  ─────────────────────────────────────────────────  │
│  # │ 等级 │ 模块 │ 描述 │ 复现步骤 │ 建议         │
│  ───┼─────┼──────┼──────┼─────────┼───────────────│
│  1 │     │      │      │         │               │
│  2 │     │      │      │         │               │
│  ...│     │      │      │         │               │
│                                                     │
│  备注：                                             │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 5.2 Bug 等级定义

| 等级 | 标签 | 定义 | 响应要求 |
|------|------|------|---------|
| **P0** | 阻断 | 应用无法启动、主功能完全失效、数据丢失 | 立即修复 |
| **P1** | 严重 | 核心功能异常、数据错误、频繁崩溃 | 24h 内修复 |
| **P2** | 中等 | 非核心功能异常、体验明显受损 | 下一个迭代 |
| **P3** | 建议 | 体验优化、边缘情况、UI 微调 | 后续迭代 |
| **P4** | 琐碎 | 文案、样式、极低频边缘情况 | 可忽略 |

### 5.3 状态定义

| 状态 | 含义 |
|------|------|
| ✅ 通过 | 实际结果完全符合预期 |
| ⚠️ 偏差 | 结果与预期有差异但不影响功能 |
| ❌ 失败 | 实际结果不符合预期，存在 Bug |
| 📝 阻塞 | 因其他 Bug 或环境原因无法测试 |
| ➖ 跳过 | 场景不适用于当前版本 |

### 5.4 测试报告模板（完整版）

> 将以下内容复制为新文件 `测试报告-YYYY-MM-DD.md` 使用

```markdown
# Usage-Float 测试报告

## 版本信息

- **应用版本**：0.1.0
- **测试日期**：____ 年 __ 月 __ 日
- **测试人员**：________________
- **测试类型**：□ 冒烟测试  □ 回归测试  □ 性能测试  □ 全量测试

## 测试环境

| 项目 | 值 |
|------|-----|
| 操作系统 | Windows ___ (__-bit) |
| Rust 版本 | rustc ___ |
| Tauri 版本 | 2.__.__ |
| WebView2 版本 | ___ |
| OpenCode 版本 | ___ |
| OpenCode 数据量 | ___ sessions |
| 硬件：CPU | ___ |
| 硬件：内存 | ___ GB |
| 硬件：磁盘 | ___ (SSD/HDD) |

## 窗口测试结果

| ID | 场景 | 步骤摘要 | 预期结果 | 实际结果 | 状态 |
|----|------|---------|---------|---------|------|
| TC-W01 | 主窗口启动 | 启动应用 | 1200×800, 仪表盘完整 | | |
| TC-W02 | 悬浮球启动 | 启动应用 | 340×200, 无边框置顶透明 | | |
| TC-W03 | 主窗口关闭 | 点击 ✕ | 进程退出 | | |
| TC-W04 | 悬浮球关闭 | 点击 ✕ | 仅浮窗关闭 | | |
| TC-W05 | 重新打开 | 最小化后恢复 | 有恢复入口 | | |
| TC-W06 | 窗口拖动 | 拖拽悬浮球 | 平滑跟随 | | |
| TC-W07 | 状态同步 | 比对两窗口数据 | 数据一致 | | |

## 数据流测试结果

| ID | 场景 | 步骤摘要 | 预期结果 | 实际结果 | 状态 |
|----|------|---------|---------|---------|------|
| TC-D01 | 正常数据流 | 启动仪表盘 | 所有卡片正确显示 | | |
| TC-D02 | 数据刷新 | 修改数据后重载 | 新数据生效 | | |
| TC-D03 | IPC 通信 | 手动 invoke | 三个 command 正常 | | |
| TC-D04 | 单 command 失败 | panic!() | 不崩溃，降级 | | |
| TC-D05 | command 不存在 | invoke 非法命令 | 优雅报错 | | |
| TC-D06 | 权限不足 | 移除 capability | 提示权限错误 | | |

## 异常测试结果

| ID | 场景 | 步骤摘要 | 预期结果 | 实际结果 | 状态 |
|----|------|---------|---------|---------|------|
| TC-E01 | 数据库不存在 | 删除 opencode.db | 降级提示，不崩溃 | | |
| TC-E02 | 数据库被占用 | OpenCode 运行中 | 等待或提示 | | |
| TC-E03 | 数据库损坏 | 破坏文件头 | 捕获错误，友好提示 | | |
| TC-E04 | 离线环境 | 断开网络 | 正常运行 | | |
| TC-E05 | 权限不足 | 低权限运行 | 权限提示 | | |

## 性能测试结果

| ID | 场景 | 目标值 | 实际值 | 状态 |
|----|------|--------|--------|------|
| TC-P01 | 冷启动时间 | ≤ 3s | ___s | |
| TC-P01 | 热启动时间 | ≤ 1.5s | ___s | |
| TC-P01 | Dashboard 首帧 | ≤ 1s | ___s | |
| TC-P01 | 悬浮球首帧 | ≤ 0.5s | ___s | |
| TC-P02 | 空闲 CPU | ≤ 0.5% | ___% | |
| TC-P02 | 拖动 CPU | ≤ 5% | ___% | |
| TC-P03 | 空闲内存 | ≤ 80 MB | ___ MB | |
| TC-P03 | 仪表盘内存 | ≤ 100 MB | ___ MB | |
| TC-P04 | 1h 后台 CPU | ≤ 0.5% | ___% | |
| TC-P04 | 1h 后台内存增长 | ≤ 10 MB | ___ MB | |

## Bug 清单

| # | 等级 | 模块 | 描述 | 复现步骤 | 建议修复 |
|---|------|------|------|---------|---------|
| 1 | | | | | |
| 2 | | | | | |
| 3 | | | | | |

## 汇总

- **总计用例**：___
- **通过**：___
- **失败**：___
- **阻塞/跳过**：___
- **P0 Bug**：___
- **P1 Bug**：___
- **P2+ Bug**：___

## 备注

```

### 5.5 CheckTest 快速冒烟清单

用于每次 CI / 构建后的快速验证：

```bash
# 命令行快速冒烟测试步骤
echo "=== Usage-Float 冒烟测试 ==="
echo "1. cargo build                   # 编译通过"
echo "2. cargo run                     # 启动无崩溃"
echo "3. 主窗口 1200×800 显示           # 视觉确认"
echo "4. 悬浮球 340×200 置顶透明        # 视觉确认"
echo "5. 仪表盘数据加载完整              # 视觉确认"
echo "6. Header 三个按钮均可点击         # 点击测试"
echo "7. 设置页正常打开                  # 点击测试"
echo "8. 悬浮球可拖拽                   # 交互测试"
echo "9. 悬浮球关闭不影响主窗口           # 交互测试"
echo "10. 主窗口关闭进程退出             # 任务管理器确认"
echo "=== 完成 ==="
```

---

## 附录：当前已知问题清单（测试前已知）

以下为架构审查中发现的**不需要测试**的已知问题，供参考：

| # | 问题 | 模块 | 严重度 | 说明 |
|---|------|------|--------|------|
| K1 | 独立浮窗数据硬编码 | 浮窗窗口 | P1 | `main.tsx` 中 FloatWidget 传入固定 82% 和 "01:42:30"，未接入数据流 |
| K2 | 数据永不刷新 | 数据层 | P1 | `refreshCache()` 无调用方，仪表盘仅在首次加载时获取数据 |
| K3 | 无错误处理 | 数据层 | P1 | `TauriProvider` 中 `Promise.all` 无 `.catch()`，任何 invoke 失败导致全量失败 |
| K4 | Dashboard 白屏 | UI | P1 | 数据加载失败时返回 `null`，用户看到空白页面，无错误提示 |
| K5 | 无 ErrorBoundary | UI | P1 | React 组件树无错误边界，任意组件异常将导致整个应用白屏 |
| K6 | 悬浮球无恢复机制 | 窗口 | P2 | 浮窗关闭后无法从主窗口重新打开 |
| K7 | 缓存无 TTL | 数据层 | P2 | 内存缓存永久有效，需手动调用 `refreshCache()` 清除 |
| K8 | `onOpenDashboard` 空实现 | 浮窗窗口 | P2 | 独立浮窗实例的「打开仪表盘」无实际行为 |
| K9 | 跨窗口状态不同步 | 架构 | P2 | 两个窗口的浮窗实例互不通信，各自维护状态 |
| K10 | 窗口关闭未使用 Tauri API | UI | P3 | `window.close()` 在 Tauri 中可能被浏览器策略阻止，应使用 `@tauri-apps/api/window` |

---

*本文档不修改任何代码，仅作为测试方案参考。*
